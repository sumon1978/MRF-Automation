
1) প্রথমে আপনার project folder এ যান

PowerShell খুলে লিখুন:

cd C:\Users\islam.munjurul\Desktop\MRF-Automation

Enter দিন।

2) Git status দেখুন
git status

এখানে আপনার নতুন file দেখাবে।

যেমন:

Untracked files:
  MRF_Automation_FINAL_CC_MOD_MRF_EDIT_FLOW_FIXED_V2.py
3) File add করুন
git add MRF_Automation_NCCD_final_V2.py

অথবা সব পরিবর্তন add করতে:

git add .
4) Commit করুন
git commit -m "Add MRF Automation FINAL V2"
5) GitHub এ push করুন
git push origin main
6) Check করুন

Push শেষ হলে:

git status

এমন দেখাবে:

Your branch is up to date with 'origin/main'.
nothing to commit, working tree clean

যদি git push origin main দেওয়ার পরে login/error আসে, সেই error এখানে paste করুন।


jodi nah thakay file folder a :



git add .
git commit -m "Add MRF Automation FINAL V2"
git push origin main

jodi thakay 

git add MRF_Automation_FINAL_CC_MOD_MRF_EDIT_FLOW_FIXED_V2.py

git commit -m "Add MRF Automation FINAL V2"
git push origin main



git ls-files

dir -Recurse -Filter "MRF_Automation_FINAL_CC_MOD_MRF_EDIT_FLOW_FIXED_V2.py"





PowerShell এ এই command দিন:

cd C:\Users\islam.munjurul\Desktop\MRF-Automation

তারপর:

git remote -v

Expected output হবে এরকম:

origin  https://github.com/sumon1978/MRF-Automation.git (fetch)
origin  https://github.com/sumon1978/MRF-Automation.git (push)

যদি এটা আসে, তাহলে নতুন file push করার জন্য:

git add .

তারপর:

git commit -m "Add MRF_Automation_CC_MOD & 5G"

তারপর:

git push origin main

যদি git remote -v এ কিছু না আসে, তাহলে GitHub link connect করতে হবে:



git remote add origin https://github.com/sumon1978/MRF-Automation.git

তারপর:

git branch -M main
git push -u origin main

এখন আগে শুধু এই command চালিয়ে result দিন:

git remote -v

তাহলে বুঝব আপনার PowerShell GitHub-এর সাথে ঠিকমতো connected কিনা।




Git a push kortay chailay :


cd "C:\Users\islam.munjurul\Desktop\MRF-Automation"


git init

git remote -v

git remote add origin https://github.com/sumon1978/MRF-Automation.git
``





Step 1: GitHub Repository খুলুন

Streamlit Cloud এ:

Manage app

এ যান।

তারপর দেখুন কোন GitHub repo connected আছে।

উদাহরণ:

mrf-automation
Step 2: GitHub এ file upload করুন

আপনার repository তে যান।

বর্তমান:

mrf_automation_professional.py

এর জায়গায় upload করুন:

MRF_Automation_FINAL_CC_MOD_MRF_EDIT_FLOW_FIXED_V2.py
Step 3: Streamlit main file change করুন

Streamlit Cloud এ যান:

https://share.streamlit.io

তারপর:

Your App
 ↓
Settings (⚙️)
 ↓
Main file path

এখন আছে:

mrf_automation_professional.py

এটা পরিবর্তন করুন:

MRF_Automation_FINAL_CC_MOD_MRF_EDIT_FLOW_FIXED_V2.py
Step 4: Save করুন

Click:

Save

তারপর:

Reboot app
Step 5: Wait করুন

Streamlit নতুন file build করবে।

শেষ হলে:

Your app is live

দেখাবে।

যদি Settings এ Main file path পরিবর্তনের option না পান:

তাহলে:

Streamlit app delete করবেন না।
GitHub repo-এর root folder এ রাখুন:
mrf-automation
│
├── MRF_Automation_FINAL_CC_MOD_MRF_EDIT_FLOW_FIXED_V2.py
├── requirements.txt

তারপর Streamlit:

Reboot app

করুন।

আরেকটি বিষয়: Streamlit Cloud-এ file name-এ space বা বড় নাম সমস্যা করে না, তবে main file path ঠিক exact হতে হবে:

MRF_Automation_FINAL_CC_MOD_MRF_EDIT_FLOW_FIXED_V2.py

.py সহ দিতে হবে।




আপনার MRF file কেন GitHub-এ আসছে না?

আগে এই command দিয়ে দেখুন:


git log --oneline --all --decorate -10


এতে দেখা যাবে আপনার MRF file কোন commit-এ আছে কিনা।

আর file tracking check:

git ls-files | findstr MRF

যদি কিছু না দেখায়, তাহলে file Git এ add হয়নি।





এখন আগে এই দুইটা command চালান:


git add .
git commit -m "Update MRF Automation files"


তারপর:

git pull origin main --rebase

যে output আসবে সেটি দিন।





GitHub repository → Streamlit Cloud → Remote run (online app)

মানে আপনার PC বন্ধ থাকলেও Streamlit app চলবে।

ধাপগুলো:

Step 1: নিশ্চিত করুন GitHub এ code push হয়েছে

PowerShell:

git status

যদি দেখায়:

Your branch is up to date with 'origin/main'
nothing to commit, working tree clean

তাহলে ঠিক আছে।

Step 2: Streamlit Cloud এ যান

এই link খুলুন:

https://share.streamlit.io/

তারপর:

Sign in with GitHub

দিয়ে আপনার GitHub account:

sumon1978

দিয়ে login করুন।

Step 3: New App তৈরি করুন

Dashboard এ:

Create app

ক্লিক করুন।

Step 4: GitHub Repository select করুন

এখানে:

Repository:

sumon1978/MRF-Automation

Branch:

main

Select করুন।

Step 5: Main file path দিন

এখানে আপনার Python file দিতে হবে।

যেমন:

MRF_Automation_FINAL_CC_MOD_MRF_EDIT_FLOW_FIXED_V2.py

খেয়াল রাখবেন:

.py extension দিতে হবে
নাম exact match হতে হবে
Step 6: Deploy করুন

Click:

Deploy!


এটাই আপনার remote online MRF Automation app হবে।